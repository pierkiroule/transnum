# L'Écho Transnumériste — webapp

Prototype statique React + Vite pour une revue numérique.

Commandes:

```
npm install
npm run dev
npm run build
npm run preview
```

La webapp est prévue pour être hébergée en statique (GitHub Pages).
# transnum